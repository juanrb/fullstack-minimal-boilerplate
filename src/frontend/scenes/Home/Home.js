// Libs
import React, {
	// useState,
} from 'react';
import PropTypes from 'prop-types';

// Local
import './Home.scss';

/**
 * @class
 * @description 
 */
const Home = props => {
	// States

	// Variables
	
	// Render
	return (
		<div className='Home'>
			Home
		</div>
	);
}

// DefaultProps
Home.defaultProps = {
	// className: 'Home'
};

// PropTypes
Home.propTypes = {
};

export default Home;
