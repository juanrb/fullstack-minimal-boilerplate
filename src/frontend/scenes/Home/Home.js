// Libs
import React, {
	// useState,
} from 'react';
import PropTypes from 'prop-types';

// Local
import './Home.scss';

// Namespace
const NS = 'Home';

/**
 * @class
 * @description 
 */
const Home = props => {
	// States

	// Variables

	// Render
	return (
		<div className={NS}>
			{NS}
		</div>
	);
}

// DefaultProps
Home.defaultProps = {
};

// PropTypes
Home.propTypes = {
};

export default Home;
