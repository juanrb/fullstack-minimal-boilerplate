import React, {
	// useState,
} from 'react';

import './Footer.scss';

const Footer = props => {
	return <div className='Footer'>Footer</div>
}

// DefaultProps
Footer.defaultProps = {
	// className: 'Footer'
};

// PropTypes
Footer.propTypes = {
};

export default Footer;
