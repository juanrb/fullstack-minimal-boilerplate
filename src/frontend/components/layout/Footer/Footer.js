import React, {
	// useState,
} from 'react';

import './Footer.scss';

// Namespace
const NS = 'Footer';

/**
 * @class
 * @description 
 */
const Footer = props => {
	// States

	// Variables

	// Render
	return (
		<div className={NS}>
			{NS}
		</div>
	);
}

// DefaultProps
Footer.defaultProps = {
};

// PropTypes
Footer.propTypes = {
};

export default Footer;
