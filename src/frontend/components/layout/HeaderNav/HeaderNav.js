// Libs
import React, {
	// useState,
} from 'react';

// Local
import './HeaderNav.scss';

// Namespace
const NS = 'HeaderNav';

/**
 * @class
 * @description 
 */
const HeaderNav = props => {
	// States

	// Variables

	// Render
	return (
		<div className={NS}>
			{NS}
		</div>
	);
}

// DefaultProps
HeaderNav.defaultProps = {
};

// PropTypes
HeaderNav.propTypes = {
};

export default HeaderNav;
